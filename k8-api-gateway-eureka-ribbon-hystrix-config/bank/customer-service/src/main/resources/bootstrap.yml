server:
  port: 8082

spring:
  application:
    name: customer-service
  cloud:
    config:
      #enabled: false
      uri: http://localhost:8888

---
spring:
  profiles: docker
  cloud:
    config:
      uri: http://config-server:8888
